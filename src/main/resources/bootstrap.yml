server:
  port: 9000
spring:
  application:
    name: erm-admin-service
  profiles:
    active: local
  shardingsphere:
    enabled: false
logging:
  file:
    name: logs/${spring.application.name}/${spring.application.name}.log
seata:
  enabled: false
mybatis-plus:
  type-aliases-package: cn.com.tcc.ofa.erm.mapper
  mapper-locations: classpath:mapper/*.xml
